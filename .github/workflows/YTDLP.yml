# Inside release_files/ directory
base_name = Path(filename).stem       # just the name without folder
zip_cmd = ["zip", "-r", "-s", f"{max_mb}m", f"{base_name}.zip", filename]
subprocess.run(zip_cmd, check=True)
Path(filename).unlink()               # delete using only filename, not full path
